# final · захищений MCP-агент специфікації. Лише те, що потрібно практиці.
#
# Python 3.10+. Перевірено 10.09.2026 на Python 3.14.6 з anthropic 0.125.0,
# mcp 2.1.1, fastembed 0.8.0, pypdf 5.9.0, python-dotenv 1.2.3. Верхні межі
# нижче відсікають мажорні версії, за якими код не перевірявся: pip не візьме
# їх сам, а людина зніме межу, коли перевірить.

# 1.x прибрав temperature з messages.create(). Код працює на обох мажорах,
# але тестуємо на 0.x — саме його ставить ця вимога.
anthropic>=0.40,<1
python-dotenv>=1.0.0

# MCP-сервер і клієнт. У SDK 2.0 клас FastMCP перейменували на MCPServer —
# код підтримує обидві версії.
mcp[cli]>=2.1,<3

# Пошук за змістом (необовʼязковий). ONNX замість torch: імпортується за секунди,
# без тисяч дрібних файлів. Без нього працює пошук лише по словах, і цього досить.
fastembed>=0.8,<1

# Читач pdf (ECMA-404, ECMA-414). Чистий Python, без важких коліс.
pypdf>=4,<6
