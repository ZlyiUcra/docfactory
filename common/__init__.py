"""СПІЛЬНЕ · читання документів і пошук — по словах і за змістом. Точок входу тут немає.

`lexical.py` перенесений з common/ модуля 4 байт у байт; `corpus.py` теж звідти,
але зведений до одного корпусу в теці corpus/. Решта написана тут: практика
модуля 4 рахує вектори моделлю e5 на torch і тримає їх у пам'яті процесу, а
серверу по stdio потрібне і швидше завантаження, і сховище, яке переживає
перезапуск, — звідси embed.py (ONNX) і vectorstore.py (Qdrant). Чому саме так —
у README практики.
"""


def nform(n: int, one: str, few: str, many: str) -> str:
    """Форма іменника при числівнику: 1 запис, 2 записи, 5 записів, 21 запис,
    12 записів. Повертає лише слово — число форматує той, хто друкує."""
    n = abs(int(n))
    if n % 10 == 1 and n % 100 != 11:
        return one
    if 2 <= n % 10 <= 4 and not 12 <= n % 100 <= 14:
        return few
    return many
